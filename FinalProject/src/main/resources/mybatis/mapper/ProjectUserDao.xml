<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.last.project.dao.IProjectUserDao">

	<select id="listDao"
		resultType="team.last.project.dto.ProjectUserDto">
		select * from user
	</select>

	<select id="viewDao"
		resultType="team.last.project.dto.ProjectUserDto">
		select * from user
	</select>

</mapper>
